class Order {
  constructor(cash, price, wrappersNeeded, type) {
    this.cash = cash;
    this.price = price;
    this.wrappersNeeded = wrappersNeeded;
    this.type = type;
  }
}

export default Order;
